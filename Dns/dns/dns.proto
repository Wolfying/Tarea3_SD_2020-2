syntax = "proto3";
package dns;
option go_package = "Dns/dns";

service DnsHandler {
    rpc GetDomain (stream inmessage) returns (stream response){}
}

message inmessage{
    string domname = 1;
}

message response {
    string ip = 1;
}